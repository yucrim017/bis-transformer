defaults:
  - prepare: config
  - data: base
  - model: transformer
  - train: base
  - mlflow: local
  - _self_

evaluate:
  run_id: null  # set to a training run_id to log Test metrics into that run

predict:
  run_id: null  # set to a training run_id to log predict results into that run
  model:
    path: null
  window:
    length: 120
    hop: 4
  case_dir: "data/processed/v1/cid_000123"
  output_dir: "outputs/predict"

explain:
  run_id: null  # set to a training run_id to log explain results into that run
  model:
    path: null
  window:
    length: 120
    hop: 4
  case_dir: "data/processed/v1/cid_000123"
  output_dir: "outputs/explain"