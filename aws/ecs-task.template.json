{
    "family": "mlflow",
    "networkMode": "awsvpc",
    "requiresCompatibilities": ["FARGATE"],
    "cpu": "1024",
    "memory": "2048",
    "executionRoleArn": "arn:aws:iam::${AWS_ACCOUNT_ID}:role/ecsTaskExecutionRole",
    "taskRoleArn": "arn:aws:iam::${AWS_ACCOUNT_ID}:role/ecsTaskRole",
    "containerDefinitions": [
        {
            "name": "mlflow",
            "image": "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/mlflow-aws:latest",
            "essential": true,
            "portMappings": [
                {
                    "containerPort": "${MLFLOW_PORT}",
                    "protocol": "tcp"
                }
            ],
            "environment": [
                {
                    "name": "MLFLOW_BACKEND_STORE_URI",
                    "value": "postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDB}"
                },
                {
                    "name": "MLFLOW_DEFAULT_ARTIFACT_ROOT",
                    "value": "s3://${S3_BUCKET}/${S3_ARTIFACTS_KEY}"
                }
            ],
            "logConfiguration": {
                "logDriver": "awslogs",
                "options": {
                    "awslogs-group": "/ecs/mlflow",
                    "awslogs-region": "${AWS_REGION}",
                    "awslogs-stream-prefix": "mlflow"
                }
            }
        }
    ]
}